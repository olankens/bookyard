spring:
  application:
    name: bookyard

  datasource:
    url: jdbc:postgresql://postgres:5432/bookyard
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true